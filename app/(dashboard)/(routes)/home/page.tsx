import React, { Fragment } from "react";

import Header from "../../_components/_common/Header";

const Home = async () => {
  return (
    <Fragment>
      <Header label="Home" />
    </Fragment>
  );
};

export default Home;
